# 🐍 Python 快速複習 Day 1-2: 基礎語法與函數

> **學習時間:** Day 1 (30分鐘) + Day 2 (30分鐘)  
> **目標:** 掌握 Python 基礎語法、字串操作、函數定義和模組使用

---

## 📅 Day 1: Python 基礎語法複習 (30分鐘)

### 學習重點
- [ ] 變數與資料型別
- [ ] 字串操作
- [ ] 列表、字典、集合
- [ ] 條件判斷與迴圈

### 實作練習

```python
# ============================================================
# 1. 資料型別與變數
# ============================================================
# Python 是動態型別語言,不需要事先宣告變數型別

name = "張三"              # str (字串) - 用雙引號或單引號都可以
age = 25                  # int (整數)
is_student = True         # bool (布林值) - 注意首字母大寫
scores = [85, 90, 78, 92] # list (列表) - 可變的有序集合
user_info = {             # dict (字典) - 鍵值對的集合
    "name": "張三",        # 鍵(key): 值(value)
    "age": 25, 
    "city": "台北"
}

# 💡 為什麼重要: AI 開發中會大量使用字典來組織 API 參數和回應資料


# ============================================================
# 2. 字串操作 (AI 提示詞會大量使用)
# ============================================================

# 基本字串
prompt = "你是一個專業的 AI 助手"

# f-string (格式化字串) - Python 3.6+ 推薦使用
# 可以在字串中直接嵌入變數,用 {} 包裹
formatted_prompt = f"角色設定: {prompt}\n使用者年齡: {age}"
# \n 是換行符號,會在輸出時產生新的一行

# 多行字串 - 使用三個引號 (""" 或 ''')
# 保留所有換行和縮排,非常適合撰寫長提示詞
multi_line = """
這是多行字串
常用於撰寫長提示詞
可以跨越多行而不需要 \n
"""

# 💡 實際應用範例:
system_prompt = f"""
你是一個專業的 {prompt}。
使用者資訊:
- 姓名: {name}
- 年齡: {age}
"""


# ============================================================
# 3. 列表操作
# ============================================================

numbers = [1, 2, 3, 4, 5]  # 建立列表

# List Comprehension (列表推導式) - Python 的強大特性
# 語法: [表達式 for 項目 in 可迭代物件 if 條件]
doubled = [n * 2 for n in numbers]  
# 等同於:
# doubled = []
# for n in numbers:
#     doubled.append(n * 2)
# 結果: [2, 4, 6, 8, 10]

# 帶條件的列表推導式
filtered = [n for n in numbers if n > 2]
# 只保留大於 2 的數字
# 結果: [3, 4, 5]

# 💡 為什麼重要: 在處理 AI 回應時,常需要過濾或轉換資料
# 例如: messages = [msg for msg in history if msg["role"] == "user"]


# ============================================================
# 4. 字典操作 (API 參數會用到)
# ============================================================

# 建立字典 - 儲存 API 配置參數
config = {
    "model": "gpt-4",        # 模型名稱
    "temperature": 0.7,      # 溫度參數
    "max_tokens": 1000       # 最大 token 數
}

# 新增或修改鍵值對
config["top_p"] = 1.0  # 使用 [] 語法直接賦值

# 安全地取得值 - 使用 get() 方法
# 如果鍵不存在,返回預設值 "default",而不會報錯
model_name = config.get("model", "default")

# 如果直接用 config["不存在的鍵"] 會引發 KeyError
# 用 get() 更安全: config.get("不存在的鍵", "預設值")

# 💡 實際應用: 呼叫 OpenAI API 時
# response = client.chat.completions.create(**config)
# ** 會將字典展開為關鍵字參數


# ============================================================
# 5. 條件判斷
# ============================================================

temperature = 0.7

# if-elif-else 結構
# 注意: Python 使用縮排(通常 4 個空格)來表示程式碼區塊
if temperature < 0.3:
    print("確定性輸出")      # temperature 在 0.0-0.29 之間
elif temperature < 0.7:
    print("平衡輸出")        # temperature 在 0.3-0.69 之間
else:
    print("創意性輸出")      # temperature >= 0.7

# 💡 實際應用: 根據不同參數調整 AI 行為
# if temperature < 0.3:
#     # 用於需要確定性的任務,如分類、資料提取
# else:
#     # 用於需要創意的任務,如寫作、腦力激盪


# ============================================================
# 6. 迴圈
# ============================================================

# for 迴圈 - 遍歷列表
for temp in [0.0, 0.5, 1.0]:
    # 每次迴圈,temp 會依序取得列表中的值
    print(f"Temperature: {temp}")
    # 輸出:
    # Temperature: 0.0
    # Temperature: 0.5
    # Temperature: 1.0

# 字典迴圈 - 同時取得鍵和值
for key, value in config.items():
    # items() 返回 (鍵, 值) 的配對
    print(f"{key}: {value}")
    # 輸出:
    # model: gpt-4
    # temperature: 0.7
    # max_tokens: 1000

# 其他常用迴圈方式:
# for key in config.keys():      # 只遍歷鍵
# for value in config.values():  # 只遍歷值
# for i in range(5):             # 遍歷 0, 1, 2, 3, 4

# 💡 實際應用: 測試不同參數組合
# for temp in [0.0, 0.5, 1.0]:
#     for model in ["gpt-3.5-turbo", "gpt-4"]:
#         # 測試不同的溫度和模型組合
```

### 💡 Day 1 核心概念總結

1. **f-string**: 最常用的字串格式化方式,語法簡潔
2. **List Comprehension**: 簡潔地處理列表,一行完成過濾和轉換
3. **字典 get()**: 安全地取得字典值,避免 KeyError
4. **縮排**: Python 用縮排表示程式碼區塊,不是用 {} 或 begin/end

### 檢查點
- [ ] 能使用 f-string 格式化字串
- [ ] 熟悉 list comprehension
- [ ] 能操作字典的增刪改查
- [ ] 理解多行字串的使用

---

## 📅 Day 2: 函數與模組 (30分鐘)

### 學習重點
- [ ] 函數定義與參數
- [ ] 預設參數與關鍵字參數
- [ ] *args 和 **kwargs
- [ ] 模組導入

### 實作練習

```python
# ============================================================
# 1. 基本函數
# ============================================================

def greet(name):
    """
    函數文檔字串 (docstring) - 描述函數功能
    使用三個引號,可以多行
    """
    return f"Hello, {name}!"
    # return 會結束函數執行並返回值

# 呼叫函數
result = greet("張三")  # result = "Hello, 張三!"


# ============================================================
# 2. 預設參數 (API 呼叫常用)
# ============================================================

def chat_with_ai(
    message: str,              # 必要參數 (沒有預設值)
    model: str = "gpt-4",      # 選用參數 (有預設值)
    temperature: float = 0.7   # 選用參數
):
    """
    與 AI 對話的函數
    
    Args:
        message: 使用者訊息 (必填)
        model: 模型名稱 (預設 gpt-4)
        temperature: 溫度參數 (預設 0.7)
    
    Returns:
        str: 格式化的回應字串
    """
    return f"Model: {model}, Temp: {temperature}, Message: {message}"

# 呼叫方式 1: 只提供必要參數,其他使用預設值
result1 = chat_with_ai("Hello")
# Model: gpt-4, Temp: 0.7, Message: Hello

# 呼叫方式 2: 覆蓋部分預設值
result2 = chat_with_ai("Hello", temperature=0.5)
# Model: gpt-4, Temp: 0.5, Message: Hello

# 呼叫方式 3: 全部指定
result3 = chat_with_ai("Hello", "gpt-3.5-turbo", 0.3)

# 💡 為什麼重要: OpenAI API 有很多選用參數,預設參數讓程式碼更簡潔


# ============================================================
# 3. 關鍵字參數
# ============================================================

# 使用關鍵字參數 (keyword arguments) 呼叫函數
# 優點: 1) 可讀性高 2) 順序不重要 3) 可以跳過某些參數
result = chat_with_ai(
    message="Hello",      # 明確指定參數名稱
    temperature=0.5       # 跳過 model,使用預設值
)

# 位置參數 vs 關鍵字參數
# chat_with_ai("Hello", 0.5)           # ❌ 錯誤! 0.5 會被當作 model
# chat_with_ai("Hello", temperature=0.5) # ✅ 正確!


# ============================================================
# 4. *args 和 **kwargs (處理不定參數)
# ============================================================

def create_prompt(*messages, **options):
    """
    *messages: 收集所有位置參數為 tuple
    **options: 收集所有關鍵字參數為 dict
    
    這讓函數可以接受任意數量的參數
    """
    print("Messages:", messages)  # tuple: ("System: ...", "User: ...")
    print("Options:", options)    # dict: {"model": "gpt-4", ...}
    
    # 實際使用
    for msg in messages:
        print(f"  - {msg}")
    
    for key, value in options.items():
        print(f"  {key} = {value}")

# 呼叫範例
create_prompt(
    "System: You are helpful",  # 第 1 個位置參數
    "User: Hello",              # 第 2 個位置參數
    model="gpt-4",              # 關鍵字參數
    temperature=0.7             # 關鍵字參數
)

# 輸出:
# Messages: ('System: You are helpful', 'User: Hello')
# Options: {'model': 'gpt-4', 'temperature': 0.7}

# 💡 實際應用: 建立靈活的 API 包裝函數
# def call_openai_api(*messages, **api_options):
#     # messages 可以是任意數量的訊息
#     # api_options 可以是任意 API 參數


# ============================================================
# 5. 型別提示 (Type Hints) - 重要!
# ============================================================

from typing import List, Dict, Optional

def process_messages(
    messages: List[Dict[str, str]],  # 列表,元素是字典
    temperature: Optional[float] = None  # 可以是 float 或 None
) -> str:  # 返回值型別是 str
    """
    處理訊息列表
    
    Args:
        messages: 訊息列表,每個訊息是 {"role": "...", "content": "..."}
        temperature: 可選的溫度參數
        
    Returns:
        處理結果字串
    """
    # Optional[float] 等同於 Union[float, None]
    if temperature is not None:
        print(f"使用溫度: {temperature}")
    
    return "Processed"

# 型別提示的好處:
# 1. 程式碼更易讀 - 一眼看出參數和返回值型別
# 2. IDE 支援 - 自動完成和錯誤檢查
# 3. 文檔作用 - 不用查看實作就知道如何使用

# 常用型別:
# str, int, float, bool - 基本型別
# List[str] - 字串列表
# Dict[str, int] - 鍵是字串,值是整數的字典
# Optional[str] - 可以是 str 或 None
# Union[str, int] - 可以是 str 或 int
# Any - 任意型別


# ============================================================
# 6. Lambda 函數 (匿名函數)
# ============================================================

# Lambda 語法: lambda 參數: 表達式
double = lambda x: x * 2
# 等同於:
# def double(x):
#     return x * 2

# 常與 map, filter 等函數一起使用
numbers = [1, 2, 3, 4, 5]

# map: 對每個元素應用函數
doubled = list(map(lambda x: x * 2, numbers))
# 結果: [2, 4, 6, 8, 10]

# filter: 過濾符合條件的元素
evens = list(filter(lambda x: x % 2 == 0, numbers))
# 結果: [2, 4]

# sorted: 自訂排序規則
users = [{"name": "張三", "age": 25}, {"name": "李四", "age": 20}]
sorted_users = sorted(users, key=lambda u: u["age"])
# 按年齡排序

# 💡 提示: 簡單的函數用 lambda,複雜的還是用 def 定義


# ============================================================
# 7. 模組導入
# ============================================================

# 方式 1: 導入整個模組
import os
api_key = os.getenv("OPENAI_API_KEY")  # 使用 os.函數名

# 方式 2: 從模組導入特定函數/類別
from pathlib import Path
project_dir = Path(".")  # 直接使用 Path,不需要 pathlib.Path

# 方式 3: 導入並重新命名 (避免名稱衝突)
from datetime import datetime as dt
now = dt.now()

# 方式 4: 導入多個
import json
from typing import List, Dict, Optional

# 環境變數 (API Key 管理)
# os.getenv(key, default) - 取得環境變數,不存在時返回 default
api_key = os.getenv("OPENAI_API_KEY", "default_key")

# 💡 最佳實踐:
# 1. 標準庫導入放最前面 (os, json, datetime)
# 2. 第三方庫放中間 (openai, requests)
# 3. 自己的模組放最後 (from my_module import ...)
# 4. 每組之間空一行

# 範例:
# import os           # 標準庫
# import json
#
# import openai       # 第三方庫
# import requests
#
# from my_app import config  # 自己的模組
```

### 💡 Day 2 核心概念總結

1. **預設參數**: 讓函數更靈活,呼叫時可以只提供必要參數
2. **型別提示**: 提高程式碼可讀性,幫助 IDE 提供更好的支援
3. ***args 和 **kwargs**: 處理不定數量的參數,建立靈活的函數
4. **Lambda**: 簡短的匿名函數,常用於 map/filter/sorted
5. **模組導入**: 組織程式碼,重複使用功能

### 檢查點
- [ ] 能定義帶型別提示的函數
- [ ] 理解預設參數的使用
- [ ] 知道如何使用 **kwargs 處理選項
- [ ] 熟悉常用模組的導入

---

## 🎯 Day 1-2 總複習

### 必須掌握的概念
1. **f-string** - 字串格式化
2. **List Comprehension** - 列表推導式
3. **字典操作** - get(), items(), keys(), values()
4. **函數定義** - def, 參數, 返回值
5. **型別提示** - 讓程式碼更清楚
6. **模組導入** - import, from...import

### 實戰練習建議

```python
# 練習 1: 建立一個 AI 配置管理函數
def create_ai_config(
    model: str = "gpt-4",
    temperature: float = 0.7,
    **extra_options
) -> dict:
    """整合 Day 1-2 的知識"""
    config = {
        "model": model,
        "temperature": temperature
    }
    config.update(extra_options)
    return config

# 使用
config = create_ai_config(temperature=0.5, max_tokens=1000)
print(config)
# {'model': 'gpt-4', 'temperature': 0.5, 'max_tokens': 1000}
```

**準備好了嗎?** 繼續前往 Day 3-4! 💪
